{
    "scripts": {
        "coveralls": "cat ./coverage/lcov.info | coveralls",
        "test": "jest --coverage"
    },
    "devDependencies": {
        "coveralls": "^3.1.0",
        "jest": "^26.0.1"
        "lzutf8": "^0.5.6",
    },
    "dependencies": {
    }
}
